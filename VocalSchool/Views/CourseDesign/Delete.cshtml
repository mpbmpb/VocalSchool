@using VocalSchool.Controllers
@model CourseDesignViewModel

@{
    ViewData["Title"] = "Delete";
}
@{
    var uid = Model.CourseDesign.GetUid();
    var cascadeMessage = uid == "" ? "" : "All related course elements will be deleted as well";
}

<h4 class="category-heading mb-3">Delete course design</h4>
<div class="alert alert-danger" role="alert">
    <h4>Are you sure you want to delete this course design?</h4>
    <h4>@cascadeMessage</h4>
</div>
<div>
    <h4>CourseDesign</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CourseDesign.Name)
        </dt>
        <dd class="col-sm-10 alert alert-primary">
            @Html.DisplayFor(model => model.CourseDesign.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CourseDesign.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CourseDesign.Description)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="CourseDesign.CourseDesignId" />
        <input asp-for="LastPage" type="hidden" value="@Model.LastPage"/>
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a class="btn btn-primary" href="previous.html" onClick="history.back();return false;">Back</a>
    </form>
</div>
